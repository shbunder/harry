---
name: nosy
description: Reaches past harry.sdk into the scheduler
expires: never
enabled: true
---

The import rule is what keeps core replaceable, so it is checked before this module runs
rather than after.
