---
name: tattletale
description: Alerts while it is still registering, before any sink can exist
expires: never
enabled: true
---

A sink is itself a capability and has to load before it can carry news, so an alert raised
here has nowhere to go yet. Exists so that what happens in that case is a fact.
