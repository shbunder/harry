"""Saved mid-edit, so it does not parse. The linters skip this whole directory."""

from harry.sdk import Context, Registry


def register(registry: Registry, context: Context) -> None
    registry.connector(
