---
name: badzone
description: Declares a timezone that is not a timezone
trigger: schedule
schedule: "0 5 * * *"
timezone: Mars/Olympus
enabled: true
---

Arrived through $HARRY_CAPABILITIES_DIR, so it never passed anybody's gate. It loads
cleanly and only fails when the scheduler reaches it.
