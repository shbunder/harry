{
  "//": "Harry, as any Claude session reaches it: HTTP MCP on localhost, behind a bearer token. The same entry works for the probe today and for real Harry later, because both answer on HARRY_PORT at /mcp — which is the point of the port having exactly one owner.",
  "mcpServers": {
    "harry": {
      "type": "http",
      "url": "http://localhost:7430/mcp",
      "headers": {
        "Authorization": "Bearer ${HARRY_API_TOKEN:-probe-token-not-a-secret}"
      }
    }
  }
}
